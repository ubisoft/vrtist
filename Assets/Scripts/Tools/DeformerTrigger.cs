using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Empty monobehavior just to identify a set of objects (because an object can only have one tag on it)
/// that can interact with the deformer planes.
/// </summary>
public class DeformerTrigger : MonoBehaviour
{
}
