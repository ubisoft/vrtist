using UnityEngine;


namespace VRtist
{
    /// <summary>
    /// A simple script to know if a game object is the root of a hierarchy of objects (children).
    /// This is used for the selection, when a root object doesn't have any collider on it but its
    /// children.
    /// This is an empty class.
    /// </summary>
    public class RootObject : MonoBehaviour
    {

    }
}